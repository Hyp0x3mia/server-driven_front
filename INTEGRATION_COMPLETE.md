# 🎮 CodePlayground 组件集成完成！

## ✅ 完成的工作

### 1️⃣ 组件创建
**文件**: `src/components/blocks/CodePlayground.tsx`

实现了两种交互模式：

#### 模式 A: Token 分词游乐场 (`tokenizer`)
- ✅ 文本输入框（支持自定义）
- ✅ 实时 Tokenization（正则分词）
- ✅ 4色循环显示（蓝/绿/紫/橙）
- ✅ 模拟 Token ID（Hash 算法）
- ✅ 统计面板（Token数、有效词、平均长度）
- ✅ Hover 提示
- ✅ 复制 JSON 功能

#### 模式 B: 超参数调优演示 (`hyperparameter`)
- ✅ Python 代码显示（动态高亮）
- ✅ Learning Rate 滑块（0.001 - 0.1）
- ✅ Momentum 滑块（0.0 - 0.9）
- ✅ 实时 SVG Loss 曲线
- ✅ 算法逻辑：
  - LR 越大 → 下降快但波动大
  - Momentum 越大 → 曲线越平滑
- ✅ 参数说明卡片

---

### 2️⃣ 类型系统注册
**文件**: `src/renderer/registry.tsx`

```typescript
export type CodePlaygroundBlock = {
  type: "CodePlayground";
  role?: string;
  content: {
    mode: 'tokenizer' | 'hyperparameter';
    initialText?: string;
    codeTemplate?: string;
  };
};

export type Block = HeroBlock | ... | CodePlaygroundBlock; // ✅ 已添加
```

**Adapter 注册**:
```typescript
const CodePlaygroundAdapter: React.FC<AdapterProps<CodePlaygroundBlock>> = ({ block }) => {
  return (
    <div className="w-full py-6">
      <CodePlayground
        type={block.content.mode}
        initialText={block.content.initialText}
        codeTemplate={block.content.codeTemplate}
      />
    </div>
  );
};

export const registry = {
  ...
  CodePlayground: CodePlaygroundAdapter, // ✅ 已注册
};
```

---

### 3️⃣ 集成到 test.json
**文件**: `public/pages/test.json`

在现有内容中添加了两个交互演示：

```json
{
  "type": "CodePlayground",
  "title": "🎮 交互演示：Token 分词",
  "content": {
    "mode": "tokenizer",
    "initialText": "Natural Language Processing enables machines..."
  }
},
{
  "type": "CodePlayground",
  "title": "🎮 交互演示：超参数调优",
  "content": {
    "mode": "hyperparameter"
  }
}
```

---

### 4️⃣ 导航系统集成
**文件**: `src/renderer/SchemaRenderer.tsx` + `src/components/layout/SidebarNav.tsx`

- ✅ `extractNavItems` 添加 `CodePlayground` 类型支持
- ✅ `SidebarNav` 添加 🎮 `Gamepad2` 图标
- ✅ 左侧导航栏会显示 CodePlayground 章节

---

## 🎨 视觉效果

### 设计风格
```
Terminal / Cyberpunk 风格
├── 深色背景 (bg-[#0f172a])
├── 等宽字体 (font-mono)
├── 绿色高亮 (emerald-400)
├── 紫色高亮 (purple-400)
└── 玻璃态边框 (border-slate-700/50)
```

### Tokenizer 模式布局
```
┌─────────────────────────────────────┐
│ 🎮 Token 分词游乐场      交互式     │
│                                     │
│ 📝 输入文本              [重置]     │
│ ┌─────────────────────────────────┐ │
│ │ Natural Language Processing... │ │
│ └─────────────────────────────────┘ │
│                                     │
│ Token 序列           (10 tokens)    │
│ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐  │
│ │Natu │ │ral  │ │Lang │ │uage │  │
│ │ID:A1│ │ID:B2│ │ID:C3│ │ID:D4│  │
│ └─────┘ └─────┘ └─────┘ └─────┘  │
│                                     │
│  10    │    8     │    5.6         │
│ Tokens │ 有效词  │ 平均长度        │
└─────────────────────────────────────┘
```

### Hyperparameter 模式布局
```
┌──────────────────────────────────────────┐
│ 🎮 超参数调优演示         交互式         │
│                                          │
│ 📝 PyTorch 优化器配置                    │
│ ┌────────────────────────────────────┐  │
│ │ optimizer = torch.optim.SGD(       │  │
│ │   model.parameters(),              │  │
│ │   lr=0.010,      ← 绿色高亮       │  │
│ │   momentum=0.5    ← 紫色高亮       │  │
│ │ )                                   │  │
│ └────────────────────────────────────┘  │
│                                          │
│ ⚙️ 超参数调优    │    📊 Loss Curve      │
│ Learning Rate    │    ╱╲               │
│ [━━━●━━━] 0.010 │   ╱  ╲   ╱╲         │
│                  │  ╱    ╲ ╱  ╲        │
│ Momentum        │  ╱      ╲      ╲     │
│ [━━━━━●━] 0.5   │                      │
│                  │   高波动=LR过大      │
└──────────────────┴   平滑=Momentum高   ┘
```

---

## 🚀 使用方式

### 访问测试页面
```bash
# 启动开发服务器
npm run dev

# 访问
open http://localhost:8080/#/page/test
```

### 页面结构
```
test.json 现在包含：
├── Hero (介绍)
├── Flashcard (自测)
├── CardGrid (NLP 基础概念)
├── Timeline (NLP 发展历程)
├── Flashcard (技术路线对比)
├── CardGrid (核心 NLP 任务)
├── Timeline (学习路径)
├── Flashcard (任务对比)
├── Cloze (填空题)
├── 🆕 CodePlayground (Token 分词演示)  ← 新增
├── 🆕 CodePlayground (超参数调优演示)  ← 新增
└── FlashcardGrid (代码练习)
```

### 左侧导航
```
目录导航
├── 📚 🤖 自然语言处理
├── 📘 NLP 基础概念
├── ⏳ NLP 发展历程
├── 📘 核心 NLP 任务
├── ⏳ NLP 学习路径推荐
├── 🎮 🎮 交互演示：Token 分词      ← 新增
└── 🎮 🎮 交互演示：超参数调优      ← 新增
```

---

## 🎯 交互功能

### Tokenizer 模式
1. **输入文本** - 在文本框中输入任何英文文本
2. **实时分词** - 自动按空格和标点符号分割
3. **Token 卡片** - 彩色卡片显示每个 Token
4. **查看 ID** - 每个 Token 显示模拟 Hash ID
5. **统计信息** - 查看 Token 总数、有效词数
6. **重置功能** - 一键恢复默认文本
7. **复制 JSON** - 导出 Token 序列

### Hyperparameter 模式
1. **拖动滑块** - 调整 Learning Rate (0.001 - 0.1)
2. **拖动滑块** - 调整 Momentum (0.0 - 0.9)
3. **观察代码** - Python 代码中的参数实时高亮
4. **查看曲线** - SVG Loss 曲线动态变化
5. **理解原理** - 阅读参数说明

---

## 📊 技术实现

### Tokenization 算法
```typescript
// 正则分词
const words = text.split(/([\s,.!?;:()]+)/).filter(t => t.trim());

// Hash 生成 ID
const hash = word.split('').reduce((acc, char) =>
  ((acc << 5) - acc) + char.charCodeAt(0), 0
);

// 4色循环
const colors = ['blue', 'green', 'purple', 'orange'];
const colorClass = colors[index % 4];
```

### Loss 曲线生成
```typescript
// 基础指数衰减
let baseLoss = Math.exp(-3 * x);

// LR 影响：越大波动越大
const lrEffect = (lr / 0.1) * 0.3 * Math.sin(20 * x);

// Momentum 影响：越大越平滑
const noise = (1 - momentum) * 0.15 * Math.sin(30 * x);

const loss = baseLoss + lrEffect + noise;
```

### React Hooks
- `useState` - 管理输入和滑块状态
- `useMemo` - 缓存计算结果
- `onChange` - 实时更新

---

## ✅ 验证清单

- [x] CodePlayground 组件创建
- [x] 类型定义添加到 registry.tsx
- [x] CodePlaygroundAdapter 实现
- [x] 注册到 registry
- [x] 集成到 test.json（2个实例）
- [x] 导航系统支持（图标 + 提取）
- [x] 构建成功验证
- [x] TypeScript 类型检查通过
- [x] 两种模式功能完整
- [x] UI 风格统一

---

## 🎉 立即体验

```bash
# 1. 启动开发服务器
npm run dev

# 2. 访问测试页面
open http://localhost:8080/#/page/test

# 3. 滚动到页面底部，找到：
#    - 🎮 交互演示：Token 分词
#    - 🎮 交互演示：超参数调优

# 4. 尝试交互
#    - 修改 Tokenizer 输入框文本
#    - 拖动 Hyperparameter 滑块
#    - 观察 Loss 曲线实时变化
#    - 查看左侧导航栏的新图标
```

**享受纯前端的 AI 交互体验！** 🚀

---

## 📝 后续优化建议

### 短期（1-2周）
1. **更多 Tokenization 策略**
   - BPE (Byte Pair Encoding) 模拟
   - WordPiece 算法演示
   - 特殊 Token 标记（[CLS], [SEP]）

2. **更多超参数**
   - Batch Size（影响曲线平滑度）
   - Weight Decay（L2 正则化）
   - Dropout Rate

### 长期（1-2月）
1. **真实后端集成**
   - 连接 HuggingFace API
   - 使用真实 Tokenizer
   - 实时训练演示（WebGL 加速）

2. **高级可视化**
   - 3D Loss 曲面图
   - 注意力权重可视化
   - 梯度流动画

3. **教学场景**
   - 预设经典示例
   - 分步骤引导教程
   - 互动测验
